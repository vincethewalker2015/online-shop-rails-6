<nav class="navbar navbar-dark navbar-expand-md bg-dark justify-content-between">
<button class="navbar-toggler" type="button" data-toggle="collapse" data-target=".dual-nav">
<span class="navbar-toggler-icon"></span>
</button>
<div class="navbar-collapse collapse dual-nav w-100 ">
<ul class="navbar-nav">
<li class="nav-item">
<% if current_user %>
  <%= link_to 'View all categories', categories_path, class: "btn btn-primary nav-but" %>
  <%= link_to 'In Cart (' + "#{current_user.cart_count}" + ')', cart_path , class: "badge badge-dark nav-cart ml-3"%> 
<% end %>
</li>

<li class="nav-item">
<div class="dropdown">
<button type="button" class="btn btn-outline-* dropdown-toggle" data-toggle="dropdown">

  
</button> <div class="dropdown-menu">
<%#= link_to 'Articles', articles_path, class: "dropdown-item mx-auto" %> 
<%#= link_to "Categories", categories_path, class: "dropdown-item mx-auto" %>
<%# if logged_in? && current_user.admin?%>
<%#= link_to "Create Article", new_article_path, class: "dropdown-item mx-auto" %>
<%#= link_to "Create a Category", new_category_path, class: "dropdown-item mx-auto"%>
<%# end %>
</div>
</div>
</li>

<%# if logged_in? %>
<li class="nav-item">
<div class="dropdown">
<button type="button" class="btn btn-outline-* dropdown-toggle" data-toggle="dropdown">
<%#= link_to current_user.username %> <%#= "(Admin)" if current_user.admin? %>
</button>
<div class="dropdown-menu">
<%#= link_to "Show User", user_path(current_user), class: "dropdown-item mx-auto" %>
<%#= link_to "Edit User", edit_user_path(current_user), class: "dropdown-item mx-auto" %>
</div>
</div>
</li>
<%# end %>

</ul>
</div>
<a href="/" class="navbar-brand mx-auto d-block text-center w-100 logo-name">Online Shop</a>
<div class="navbar-collapse collapse dual-nav w-100">
<ul class="nav navbar-nav ml-auto">
<%# if current_user %>

<%# end %>
<% if current_user %> 
<li class="nav-user mr-2 mt-2"><%= current_user.name %> <%= "(Admin)" if current_user.admin? %></li>
<li><%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "btn btn-danger nav-but" %></li>
<% else %>
<li><%= link_to "Log in", user_session_path, class: "btn btn-primary nav-but" %></li>
<% end %>
</ul>
</div>
</nav>

